from django.apps import AppConfig

class ReportesIncidenciaConfig(AppConfig):  # Cambiado a 'I' mayúscula
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.reportesIncidencia'